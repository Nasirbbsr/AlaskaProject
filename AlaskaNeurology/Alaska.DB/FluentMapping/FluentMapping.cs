using Microsoft.Data.Entity;

namespace Alaska.DB.FluentMapping
{
    public class FluentMapping
    {
        public FluentMapping(ModelBuilder dbModelBuilder)
        {
           // dbModelBuilder.Configurations.Add(new PatientMapping());
        }
    }
}
